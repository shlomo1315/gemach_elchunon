# 📧 הפעלת מערכת המיילים — גמ"ח זכרון אהרן

המערכת שולחת מייל HTML מעוצב **בכל פעולה** (הפקדה, משיכה, חבר חדש, עריכה,
מחיקה, שיקים, אישור בקשות, ייבוא) מהכתובת
**noreply@gemach.shlomo4you.com** דרך שירות Resend:

- **למנהל** — על כל פעולה (ניתן לכיבוי בדף "מיילים" → הגדרות).
- **לחבר** — אם הפעולה קשורה אליו **ויש לו כתובת מייל שמורה** בכרטיס.

בנוסף, בדף **"מיילים"** במערכת: יומן כל המיילים שנשלחו (כולל כשלים
ושליחה חוזרת), שליחה יזומה של הודעה לחבר בודד או לכולם, והגדרות שליחה.

> בלי ההגדרה למטה המערכת עובדת רגיל — מיילים לא נשלחים, והכשלים
> מופיעים ביומן עם השגיאה `not_configured`.

---

## הפעלה — 4 צעדים (חד-פעמי)

### 1. חשבון Resend ואימות הדומיין
1. נרשמים בחינם ב-https://resend.com (תוכנית חינם: ~3,000 מייל/חודש, 100/יום).
2. בתפריט **Domains** → **Add Domain** → מקלידים: `gemach.shlomo4you.com`.
3. Resend יציג 2–3 רשומות DNS (בעיקר TXT/CNAME של DKIM ו-SPF).
   נכנסים לממשק ניהול ה-DNS של הדומיין `shlomo4you.com` (איפה שקנית אותו
   או איפה שהוא מנוהל) ומוסיפים את הרשומות **בדיוק** כפי שמוצג ב-Resend.
4. חוזרים ל-Resend ולוחצים **Verify DNS Records**. (לפעמים לוקח כמה דקות
   עד שעה שה-DNS מתעדכן — אם נכשל, מחכים ומנסים שוב.)

### 2. יצירת API Key
ב-Resend → **API Keys** → **Create API Key** → מעתיקים את המפתח
(מתחיל ב-`re_`). המפתח מוצג פעם אחת בלבד.

### 3. משתני סביבה ב-Vercel
ב-**Vercel → הפרויקט → Settings → Environment Variables**, מוסיפים:

| שם | ערך |
|----|-----|
| `RESEND_API_KEY` | המפתח שהעתקת (`re_...`) |
| `ADMIN_EMAIL` | המייל שלך — לשם יגיעו התראות המנהל |
| `EMAIL_FROM` | (אופציונלי) ברירת מחדל: `גמ"ח זכרון אהרן <noreply@gemach.shlomo4you.com>` |

> משתני Gmail ישנים (`GMAIL_*`) לא נחוצים — אפשר למחוק אם הוגדרו.

### 4. טבלאות ופריסה
1. ב-**Supabase → SQL Editor** מריצים פעם אחת את `supabase/email-schema.sql`
   (יוצר את `email_log` ו-`email_settings`).
2. ב-Vercel → **Deployments** → על הפריסה האחרונה → **Redeploy**
   (כדי שמשתני הסביבה ייטענו).

---

## חלופה: שליחה דרך Gmail

הקוד תומך גם בשליחה דרך חשבון Gmail (אם `RESEND_API_KEY` לא מוגדר):
- עם OAuth2: `GMAIL_USER` + `GMAIL_CLIENT_ID` + `GMAIL_CLIENT_SECRET` + `GMAIL_REFRESH_TOKEN`.
- או עם סיסמת אפליקציה (דורש אימות דו-שלבי): `GMAIL_USER` + `GMAIL_APP_PASSWORD`.

---

## איך זה עובד טכנית
- כל פעולה קוראת ל-`notify()` (צד לקוח) שמפעיל את `/api/notify` (צד שרת).
- השרת בודק את ההגדרות (דף "מיילים" → הגדרות), שולח דרך Resend
  (`lib/mailer.ts`), ורושם כל שליחה — הצלחה או כשל — בטבלת `email_log`.
- השליחה היא "שגר ושכח" — אם מייל נכשל, **הפעולה במערכת כבר נשמרה**.
  הכשל מופיע ביומן ואפשר לשלוח שוב בלחיצה.
- שליחה יזומה: דף "מיילים" → "שליחה יזומה" → `/api/send-email`.

## כדי שגם החברים יקבלו מייל
צריך שלכרטיס החבר תהיה כתובת מייל. אפשר להוסיף מייל בכרטיס החבר.
חברים בלי מייל — רק המנהל יקבל התראה לגביהם.

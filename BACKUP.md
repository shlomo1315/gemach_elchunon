# גיבוי ושחזור

## מה נשמר

הגיבוי הוא קובץ JSON יחיד הכולל: חברים, פעולות, שיקים, בקשות תיקון,
בקשות חברים, פעולות מחוקות, משתמשי המערכת, מסמכים והגדרות המייל.

**לא** נשמר יומן המיילים (`email_log`) — הוא מכיל את ה-HTML המלא של כל
הודעה שנשלחה, מנפח את הקובץ פי כמה ואינו נתון שצריך לשחזר. גם קודי
איפוס סיסמה (`password_resets`) אינם נשמרים, בהיותם זמניים.

מסמכים שחברים העלו נשמרים בתוך הקובץ בקידוד base64, כך שגם הם משוחזרים.

## שימוש יומיומי

**הגדרות ← 💾 גיבוי ושחזור** (מנהל בלבד):

- **הורד גיבוי למחשב** — קובץ יורד ישירות לדפדפן.
- **שלח גיבוי למייל עכשיו** — נשלח לכתובת המוגדרת ב-`ADMIN_EMAIL`.

בנוסף, גיבוי נשלח אוטומטית למייל המנהל אחת ליום (ראה הקמה למטה).

## שחזור

השחזור מוחק את כל הנתונים הקיימים ומחליף אותם בתוכן הקובץ. לכן הוא
מוגן בכמה שלבים:

1. בחירת הקובץ — הוא נבדק לפני שנוגעים במסד.
2. **תצוגה מקדימה** — טבלה המראה כמה רשומות יש כעת בכל טבלה וכמה יהיו
   אחרי, כולל סימון באדום היכן ייגרע מידע.
3. **הקלדת המילה "שחזר"** — לחיצה בלבד אינה מספיקה.
4. **גיבוי אוטומטי לפני** — המצב הנוכחי נשלח למייל לפני הדריסה. אם
   השליחה נכשלת, השחזור **מבוטל** ולא נדרס דבר.
5. השחזור רץ בטרנזקציה אחת: או שהכל מצליח, או שהמסד נשאר כפי שהיה.

## הגיבוי היומי — איך הוא עובד

**אין צורך בהגדרה ב-Railway.** הגיבוי מוגדר ופועל מתוך המערכת עצמה:

**הגדרות ← 💾 גיבוי ושחזור** מאפשר לקבוע את כתובת המייל לקבלת הגיבוי
(ריק = מייל המנהל), ולהפעיל או לכבות את הגיבוי האוטומטי.

בכל כניסה של מנהל למערכת נבדק אם עברה יממה מאז הגיבוי האחרון, ואם כן
הוא נשלח ברקע. השרת הוא שמחליט, ולכן כניסות מרובות באותו יום מייצרות
גיבוי אחד בלבד.

### המגבלה שחשוב להכיר

הגיבוי מופעל על ידי כניסה למערכת. **אם לא נכנסים במשך כמה ימים, לא
נוצרים גיבויים באותם ימים.** בפועל, במערכת שנמצאת בשימוש יומיומי זה
כמעט לא קורה — ואם כן נוצר פער של 3 ימים ומעלה, מוצגת התראה בולטת
במסך ההגדרות.

מי שרוצה גיבוי שאינו תלוי בכניסה כלל — יכול להוסיף גם את שירות ה-cron
המתואר בהמשך. השניים אינם מתנגשים: השרת שולח רק אם באמת הגיע הזמן.

## הקמת cron ב-Railway (רשות)

### 1. משתנה סביבה משותף

בשירות האפליקציה (`gemach-app`) הוסף:

```
CRON_SECRET=<מחרוזת אקראית ארוכה>
```

ליצירת ערך: `openssl rand -base64 32`

זהו הסוד שמאפשר להרצה האוטומטית לפעול בלי התחברות. אל תשתמש בערך קצר
או צפוי.

### 2. שירות ה-Cron

ב-Railway → **New** → **GitHub Repo** → אותו מאגר. בשירות החדש:

| הגדרה | ערך |
|---|---|
| **Start Command** | `npm run backup:daily` |
| **Cron Schedule** | `0 3 * * *` (כל יום ב-03:00) |

משתני סביבה לשירות הזה:

```
CRON_SECRET=<אותו ערך בדיוק כמו באפליקציה>
BACKUP_URL=https://gemach.shlomo4you.com
```

> **חשוב:** שירות cron ב-Railway חייב לצאת בסיום הריצה. הסקריפט
> `railway/daily-backup.mjs` אכן קורא ל-`process.exit()` — אל תשנה זאת,
> אחרת הרצות עתידיות יידלגו.

### 3. בדיקה

לחץ **Deploy** בשירות ה-cron להרצה מיידית. בלוגים אמור להופיע:

```
✅ הגיבוי נשלח: gemach-backup-2026-08-17.json · 412 KB · חברים 99 · פעולות 777 · שיקים 12 · 2.3s
```

אם מופיע `❌ חסר CRON_SECRET` — המשתנה לא הוגדר בשירות ה-cron.
אם מופיע `HTTP 403` — הערך אינו זהה לזה שבאפליקציה.

## הרצה ידנית מהמחשב

```bash
CRON_SECRET="<הסוד>" BACKUP_URL="https://gemach.shlomo4you.com" npm run backup:daily
```

## המלצה

שמור את קבצי הגיבוי גם מחוץ לתיבת המייל — בתיקייה במחשב או בענן.
גיבוי שנמצא רק במקום אחד אינו גיבוי.
